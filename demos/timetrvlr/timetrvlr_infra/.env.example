# Environment variables for Amplify CDK deployment
# Fill in the required values before running

# GitHub repository details
GITHUB_OWNER=wellcomecollection
GITHUB_REPO=wc_simd

# Amplify app name (optional, defaults to 'timetrvlr')
AMPLIFY_APP_NAME=timetrvlr

# GitHub authentication - provide either GITHUB_TOKEN_SECRET_NAME or GITHUB_TOKEN
# GITHUB_TOKEN_SECRET_NAME=your-github-token-secret-name-in-aws-secrets-manager
GITHUB_TOKEN=your_github_personal_access_token_here

# Branch to deploy (optional, defaults to 'main')
BRANCH=main

# Backend base URL for the Next.js app (optional)
NEXT_PUBLIC_TIMETRVLR_BACKEND_BASE=https://your-backend-url.com